import React from "react";
import { View, Text } from "react-native";

const ShipsScreen = () => {
  return (
    <View style={{ flex: 1, justifyContent: "center", alignItems: "center" }}>
      <Text>Liste des ships </Text>
    </View>
  );
};

export default ShipsScreen;
